def printFunc():
    # //This is a comment omg so exciting

    lis = ["This", "code", "has", "no", "personality", "at", "all"]

    print("But its readable i guess")

    string = """This code was indented with
    tabs, INGSOC fixed it"""
    q = len(string)
    for i in range(0, 50):
        q += len(lis)


var = "Ive been sent to a re-education camp for comparing bools directly"
if isinstance(var, str) is True:
    printFunc()

print("Big brother 'asked' me to end this code with a newline")
